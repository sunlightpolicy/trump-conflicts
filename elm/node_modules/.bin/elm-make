#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../elm/binwrappers/elm-make" "$@"
  ret=$?
else 
  node  "$basedir/../elm/binwrappers/elm-make" "$@"
  ret=$?
fi
exit $ret
