#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../elm/binwrappers/elm-reactor" "$@"
  ret=$?
else 
  node  "$basedir/../elm/binwrappers/elm-reactor" "$@"
  ret=$?
fi
exit $ret
